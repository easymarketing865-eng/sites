<section class="hero-section">
    <div class="hero-text">
        <h1 class="hero-head headline">
            <div class="hero-line">
                <span>All around creative crew</span>
                <img
                    src="/photos/affordable.jpg"
                    alt="Affordable production"
                    class="hero-line-img"
                />
            </div>
            <div class="hero-line">
                <span>with Stunning video production</span>
                <img
                    src="/photos/stunning.jpg"
                    alt="Stunning visual"
                    class="hero-line-img"
                />
            </div>

            <div class="hero-line">
                <span>At very welcoming rates</span>
                <img
                    src="/photos/productions.jpg"
                    alt="Production scene"
                    class="hero-line-img"
                />
            </div>
            <div class="hero-line">
                <span>In the heart of Central Asia</span>
                <img
                    src="/photos/showreel.jpg"
                    alt="Showreel"
                    class="hero-line-img"
                />
            </div>
        </h1>
        <!-- <h1 class="showreel-heading">Where stories meet the wild.</h1> -->
    </div>
</section>

<style>
    .hero-section {
        width: 100%;
        max-width: var(--main-maxwidth);
        margin: 0 auto 1.35em auto;
        padding: 0 2rem;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        position: relative;
        box-sizing: border-box;
    }
    .hero-text {
        max-width: none;
        padding-right: 0.5em;
        z-index: 1;
        width: 100%;
    }
    .hero-head {
        font-size: var(--font-size-lg);
        position: relative;
        letter-spacing: 0.03em;
        font-family: "Manrope", var(--font-sans);
        font-weight: 800;
        color: var(--brand-red);
        text-transform: uppercase;
        margin: 0 0 0.25em 0;
        line-height: 1.06;
        display: flex;
        flex-direction: column;
        gap: 1rem;
        width: 100%;
    }
    .hero-tagline {
        margin: 1.15em 0 0 0;
        font-size: var(--hero-tagline-size);
        line-height: 1.25;
        letter-spacing: 0.015em;
        color: var(--brand-black);
        max-width: 28ch;
    }
    .hero-photo {
        width: 100%;
        /* max-width: 390px; */
        margin-top: 1.2em;
        /* height: 50px; */
  
        background: var(--site-bg);
        box-shadow: 0 4px 32px 0 #0002;
        object-fit: cover;
        display: block;
    }
    .hero-line {
        display: flex;
        align-items: center;
        justify-content: start;
        gap: 1rem;
    }
    .hero-line:nth-child(even) {
        flex-direction: row-reverse;
    }
    .hero-line span {
        font-size: var(--font-size-md);
        flex: 0 1 35%;
    }
    .hero-line-img {
        height: 100px;
        flex: 0 1 45%;
        min-width: 200px;
        object-fit: cover;
    }
    @media (max-width: 768px) {
        :root {
            --font-size-xl: 36px;
            --font-size-lg: 28px;
            --font-size-md: 18px;
            --font-size-base: 16px;
        }
        .hero-line,
        .hero-line:nth-child(even) {
            /* flex-direction: column; */
            flex-wrap: wrap;
            align-items: center;
            gap: 1rem;
            /* margin-bottom: 1rem; */
        }
        .hero-line span {
            font-size: var(--font-size-base);
            max-width: 48%;
        }
        .hero-line-img {
            max-width: 50%;
            /* height: auto; */
            /* max-height: 85px; */
            min-width: 25%;
        }
    }
    @media (max-width: 420px) {
        :root {
            --font-size-xl: 32px;
            --font-size-lg: 24px;
            --font-size-md: 18px;
            --font-size-base: 16px;
        }
        .hero-section {
            padding: 0 0.5rem;
        }
        .hero-text {
            padding-right: 0.1em;
        }
        .hero-photo {
            margin-top: 0.9em;
        }
    }
</style>
