<header class="main-header">
    <a class="brand-logo" href="/">
        <img src="/photos/logo.svg" alt="easy. production" class="logo-img" />
        
    </a>
    
    <nav class="main-nav">
        <ul class="nav-list">
            <li><a href="/portfolio" class="nav-link">Portfolio</a></li>
            <li><a href="/contact" class="nav-link">Contact</a></li>
        </ul>
        <button class="mobile-menu-btn" aria-label="Toggle menu">
            <span></span>
            <span></span>
            <span></span>
        </button>
    </nav>
</header>

<style>
    .main-header {
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: space-between;
        background: transparent;
        box-sizing: border-box;
        position: relative;
        padding-bottom: 2rem;
        z-index: 10;
    }
    .main-header .brand-logo {
        
        text-decoration: none;
        background: transparent;
        border: 0;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        line-height: 1.05;
    }
    .logo-img {
        width: 50px;
        height: auto;
        display: block;
   
   
    }

    .brand-subtitle {
        color: var(--brand-black, #666);
        margin-top: 0.2em;
        letter-spacing: 0.05em;
        text-transform: lowercase;
        opacity: 0.8;
    }

    .main-nav {
        display: flex;
        align-items: center;
        gap: 2em;
    }

    .nav-list {
        display: flex;
        list-style: none;
        margin: 0;
        padding: 0;
        gap: 1em;
        align-items: center;
    }

    .nav-link {
        color: var(--brand-black, #333);
        text-decoration: none;
    }

    .nav-link:hover {
        color: #99242A;
    }

    .mobile-menu-btn {
        display: none;
        flex-direction: column;
        gap: 3px;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0.5em;
    }

    .mobile-menu-btn span {
        width: 24px;
        height: 3px;
        background: var(--brand-black, #333);
        transition: all 0.3s ease;
    }


    /* Mobile-first responsive design */
    @media (min-width: 768px) {
        .logo-img {
            width: 80px;
        }
        
        .nav-list {
            gap: 1.5em;
        }
        
        .nav-link {
            font-size: 0.9rem;
        }
    }
</style>
