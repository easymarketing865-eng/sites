<header class="main-header">
    <a class="brand-logo" href="/">
        <img src="/photos/logo.svg" alt="easy. production" class="logo-img" />
    </a>
    
    <nav class="main-nav">
        <ul class="nav-list">
            <li><a href="/portfolio" class="nav-link">Portfolio</a></li>
        </ul>
        <button class="mobile-menu-btn" aria-label="Toggle menu">
            <span></span>
            <span></span>
            <span></span>
        </button>
    </nav>
    
    <a href="/contact" class="hire-us-btn">Hire us</a>
</header>

<style>
    .main-header {
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: space-between;
        background: transparent;

        box-sizing: border-box;
        position: relative;
        padding-bottom: 2rem;
    }
    .main-header .brand-logo {
        text-decoration: none;
        background: transparent;
        border: 0;
     
        line-height: 1.05;
        transition: opacity 0.13s;
    }
    .main-header .brand-logo:focus-visible,
    .main-header .brand-logo:hover {
        opacity: 0.8;
        outline: none;
    }
    .logo-img {
        width: 80px;
        height: auto;
        display: block;
        mix-blend-mode: multiply;
        background: transparent;
    }

    .main-nav {
        display: flex;
        align-items: center;
        gap: 2em;
    }

    .nav-list {
        display: flex;
        list-style: none;
        margin: 0;
        padding: 0;
        gap: 1em;
        align-items: center;
    }

    .nav-link {
        font-family: var(--font-sans);
        font-size: 0.875rem;
        font-weight: 600;
        color: var(--brand-black, #333);
        text-decoration: none;
        transition: color 0.2s ease;
        position: relative;
    }

    .nav-link:hover {
        color: var(--brand-red, #A00000);
    }

    .nav-link::after {
        content: '';
        position: absolute;
        bottom: -4px;
        left: 0;
        width: 0;
        height: 2px;
        background: var(--brand-red, #A00000);
        transition: width 0.2s ease;
    }

    .nav-link:hover::after {
        width: 100%;
    }



    .mobile-menu-btn {
        display: none;
        flex-direction: column;
        gap: 3px;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0.5em;
    }

    .mobile-menu-btn span {
        width: 24px;
        height: 3px;
        background: var(--brand-black, #333);
        transition: all 0.3s ease;
    }

    .hire-us-btn {
        display: inline-block;
        padding: 0.5em 1em;
        font-size: 0.8rem;
        font-weight: 700;
        color: #fff;
        background: linear-gradient(90deg, #a80000 0%, #740000 100%);
        border: none;
        border-radius: 2em;
        box-shadow:
            0 2px 12px rgba(168, 0, 0, 0.18),
            0 1px 3.5px #720000 inset;
        cursor: pointer;
        text-decoration: none;
        transition:
            background 0.17s,
            transform 0.1s,
            box-shadow 0.17s;
        letter-spacing: 0.03em;
    }
    .hire-us-btn:hover,
    .hire-us-btn:focus-visible {
        background: linear-gradient(90deg, #740000 0%, #a80000 100%);
        box-shadow:
            0 4px 24px rgba(168, 0, 0, 0.26),
            0 1px 7px #720000 inset;
        transform: translateY(-2px) scale(1.04);
        outline: none;
    }
    /* Mobile-first responsive design */
    @media (min-width: 480px) {
        .main-header {
            
        }
        
        .logo-img {
            width: 100px;
        }
        
        .hire-us-btn {
            font-size: 0.875rem;
            
        }
    }
    
    @media (min-width: 768px) {
        .main-header {
            padding: 
        }
        
        .logo-img {
            width: 120px;
        }
        
        .nav-list {
            gap: 1.5em;
        }
        
        .nav-link {
            font-size: 0.9rem;
        }
        
        .hire-us-btn {
            font-size: 0.9rem;
            padding: 0.6em 1.4em;
        }
    }
    
    @media (min-width: 1024px) {
        .main-header {

        }
        
        .logo-img {
            width: 150px;
        }
        
        .nav-link {
            font-size: 1rem;
        }
        
        .hire-us-btn {
            font-size: 1rem;
            padding: 0.7em 1.6em;
        }
    }
</style>
